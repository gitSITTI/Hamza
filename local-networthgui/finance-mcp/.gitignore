node_modules/
dist/
.env
.env.*
!.env.example

# Personal financial data must never be committed. Only the *.example.json
# template is tracked; the live store below is ignored.
data/finance-profile.json
