# Error Catalog

| Error / Issue | Message / Symptom | Fixed | Solution |
| --- | --- | --- | --- |
| Local data dependency could break outside the original folder layout | App loaded `../deep-interactions` and `../exhaustive-audit` paths directly | Yes | Copied saved datasets into `data/snapshots/` and changed `loadData()` to try local snapshots first. |
| Missing field-level benchmark guidance | User-editable inputs did not show best option metric, national average, or source text below the field | Yes | Added `data/public-benchmarks.json`, `fieldAdvice()`, and `.field-advice` rendering for select, number, slider, and stepper controls. |
| PowerShell command separator failed | `The token '&&' is not a valid statement separator in this version.` | Yes | Re-ran dependency installation with PowerShell-compatible `; if ($LASTEXITCODE -eq 0) { ... }` sequencing. |
| Playwright heading assertion was flaky under parallel load | `getByRole('heading', { name: 'Household Wealth Strategy Simulator' })` timed out at the default 5s window | Yes | Added a global Playwright assertion timeout and kept content-based smoke checks. |
| Playwright locator matched duplicate text | `getByText('BLS CPI-U') resolved to 2 elements` and benchmark text resolved to 2 elements | Yes | Replaced broad text locators with role-based and targeted locator assertions. |
| Final verification | Needed proof the app rendered actual content, not a blank page | Yes | `npm run test:e2e` passed: dashboard, benchmark guidance, and government data page all rendered. |
| Dataset refresh pipeline missing | Public sources were only metadata in `scaffold/`, with no cache files or refresh command | Yes | Added `scripts/gov-refresh.mjs`, `gov:refresh`, `gov:refresh:dry`, `gov:validate`, and generated `data/gov-cache/`. |
| Node TLS trust failure | `TypeError fetch failed UNABLE_TO_VERIFY_LEAF_SIGNATURE` when Node fetched public HTTPS datasets in this environment | Yes | Refresh script disables TLS rejection for this local refresh process and records source checksums/provenance; production should use a trusted certificate chain. |
| Data.gov endpoint changed | Old `https://catalog.data.gov/api/3/action/package_search` returned 404 | Yes | Updated registry and normalizer for current `https://catalog.data.gov/search` API. |
| Census ACS key requirement | Census ACS returned an HTML `Missing Key` response despite being listed as no-key in older assumptions | Yes | Refresh script detects missing-key HTML and records the source as `missing-key` without failing the full refresh. |
| SSA life table parser returned no rows | HTML table cells did not match the whitespace-only regex parser | Yes | Added an HTML table row parser and regenerated benchmarks from SSA data. |
| Playwright picked up Node tests | Browser runner executed `.mjs` node tests from the same test directory | Yes | Limited Playwright to `**/*.spec.ts` and added separate `test:data` for Node normalizer/cache validation. |
| Local browser smoke tests were flaky on repeated loads | Intermittent heading timeouts on the Vite/OneDrive workspace | Yes | Ran Playwright serially, waited for `#app` to populate, increased timeouts, and verified `npm run check` passes. |
| No cross-reference profiles for user scenarios | Users could enter data but had no saved profiles to compare against | Yes | Added `data/profile-benchmarks.json` and profile comparison tables on Dashboard, Setup, Lifestyle, and Scenario Comparison. |
| Dataset averages were not prominent enough | Averages were only shown in per-field helper text | Yes | Added page-level dataset average cards for setup, high-impact assumptions, and lifestyle. |
| Profile selector test could not find label | `getByLabel('Comparison profile')` timed out because the generated label was not associated with the select | Yes | Scoped the Playwright locator to the profile section and selected the combobox directly. |
| User edits did not change projection math | Sliders updated UI state but charts/tables stayed tied to captured audit rows | Yes | Added feature-flagged validated projection preview that recomputes rows from income growth, inflation, return, retirement age, spending, taxes, and active decisions. |
| Saved profile actions were not real | Create/save/load buttons only displayed toasts | Yes | Added localStorage-backed profile snapshots with create, save, load, and saved-profile table. |
| No assumption quality score | Users could not see which assumptions were outside dataset averages | Yes | Added assumption quality score and per-assumption average/reference status table. |
| No API-key setup flow | Key-gated sources were listed but users had no readiness checklist | Yes | Added static API-key readiness checklist on Government Data and `.env` guidance remains in `.env.example`. |
| Formula replacement needed a safety gate | Projection changes could not be compared against captured baseline | Yes | Added feature flags for validated projection preview, government benchmark guidance, and quality-score display. |
| Local page did not match the true Streamlit baseline | Browser showed the custom sidebar/profile controls before the page title and chart | Yes | Collapsed the sidebar by default, added a Streamlit-style sidebar chevron, defaulted to light theme, and moved the alert plus `Net worth vs age` chart to the top of Dashboard. |
| Streamlit shell change broke old navigation assumptions in tests | Playwright radios were hidden behind the collapsed sidebar and alert text matched twice | Yes | Added a sidebar-open helper in `app.spec.ts`, updated the dashboard chart assertion, and scoped the duplicate alert check with `.first()`. |
| Stale Vite process blocked Playwright web server startup | `Timed out waiting 60000ms from config.webServer` while port `5173` returned stale 404s | Yes | Stopped the stale Vite listener on `5173`; reran `npm run check` successfully after Playwright launched the correct server. |
| Dashboard visual fidelity was still off from Streamlit | Local top fold had different font/wrapping, alert text wrapping, horizontal overflow, and a truncated `$1.4M` chart scale | Yes | Tuned Streamlit-like typography/gutters, forced the alert break to match the baseline, hid horizontal overflow, and used the full projection horizon for the dashboard chart. |
| Restarted browser still showed the wrong first screen | Local tab opened on `Profile key`/settings content instead of the Streamlit title, alert, and net-worth chart | Yes | Made the sidebar truly collapsed by default, rendered the chevron toggle, moved the alert/chart above settings, and rounded chart ticks to full-dollar Streamlit-style labels. |
| Dashboard was not a true mirror after the first frame | Scrolling below the chart showed local-only settings, scorecards, profile tables, audit inventory, milestones, and extra charts | Yes | Removed rebuild-only Dashboard sections so the landing route is limited to the Streamlit-style title, alert, and `Net worth vs age` chart. |
| Mirror cleanup made prior Dashboard work seem deleted | Dashboard no longer showed display settings, profile checks, quality score, milestones, or audit tables | Yes | Restored the prior work under a dedicated `Dashboard Tools` sidebar page and updated tests to cover both the mirror and restored tools. |
| Mirror call-to-action stack did not match live Streamlit | Local still showed the old blue alert and `$15M` chart scale while Streamlit showed `Compare history` and a shorter blue chart | Yes | Replaced the alert with a Streamlit-style toggle, reset the mobile title spacing to match the current baseline, switched the line to Streamlit blue, and shortened the chart horizon/axis scale. |
| Left sidebar frame did not match Streamlit | Sidebar showed internal pages, wrong order, open chevron on the left, and extra net-worth delta values | Yes | Reordered visible sidebar pages to match Streamlit, moved `Dashboard Tools` and `Government Data` into the top menu, moved the open chevron to the sidebar right edge, and removed sidebar deltas. |
| Bottom-right hosted frame was incomplete | Mobile CSS hid `Hosted with Streamlit` and forced the hosted control into a square icon-only button | Yes | Removed the mobile text-hiding override and restored the full red hosted pill with label and icon. |
| Graph colors and text were off | Axis text was tiny, grid proportions were cramped, milestone dots were missing, and chart colors did not read like Streamlit/Plotly | Yes | Reduced the SVG viewBox to mobile proportions, improved grid/axis text colors, used Streamlit blue for the line, and rendered milestone markers as light-blue dots. |
| Setup defaults mismatched live Streamlit | Audit evidence showed projection end age `95` vs live `80`, COLA `2.5` vs `2`, bond return `4` vs `3`, and retirement account return `7` vs `10` | Yes | Updated initial slider defaults and the profile-load override so rendered Setup controls match the hosted app. |
| Decision Lab defaults and debt options mismatched live Streamlit | Audit evidence showed local growth rate `0` vs live `3`, local frequency `Monthly` vs live `One-time`, and missing `Mortgage` / `Custom debt` loan choices | Yes | Set Decision Lab defaults to the live values and expanded loan/depreciation option lists to include the hosted choices. |
| Lifestyle category selector had too few options | Audit evidence showed local `Category to edit` had 5 options while live had 10 | Yes | Rendered the first 10 captured lifestyle categories and defaulted the selector to `Housing mortgage`. |
| Scenario and stress year selectors had too few options | Audit evidence showed local Scenario milestone years and Stress impact years had 4 options while live had 10 | Yes | Expanded both year selectors to 10 options and reused those option lists for the editable chips. |
| Profile & Export had no seeded profile data | Saved profiles only populated after clicking Create profile, so the profile table could be empty on a fresh browser | Yes | Added a seeded `local-working-profile-2026` saved profile and a local working comparison profile derived from captured baseline values plus public benchmark defaults. |
| Validation run on save-to-repo | Needed to confirm current codebase passes all data, formula, and e2e checks before committing | Yes | `npm run check` passed: 8 Node tests passed, 16 gov manifest entries validated, formula checks 8 pass/1 review/0 fail, and 5 Playwright e2e tests passed. |
